{
  "name": "signalonly",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "description": "SignalOnly Chrome extension build / lint / test scripts.",
  "scripts": {
    "check": "node scripts/check.js",
    "test": "node --test \"tests/*.test.js\""
  }
}
